# Backend API — shares the existing center-kitchen-backend (Render), same as
# the abt project. Namespaced under /api/modu-high; those routes still need
# to be added in center-kitchen/backend/routes once this app's features are defined.
VITE_API_URL=https://center-kitchen-backend.onrender.com/api/modu-high
